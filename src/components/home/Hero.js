import React from 'react';
import PropTypes from 'prop-types';
import { Button } from 'antd';
import PageHeader from '../util/PageHeader';

const Hero = () => {
  return (
    <div>
      <PageHeader text="Cheaper roses. Better bouquets." />
    </div>
  );
};

export default Hero;
