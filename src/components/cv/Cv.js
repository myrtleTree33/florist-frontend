import React from 'react';
import PropTypes from 'prop-types';

import MarginDiv from '../util/MarginDiv';

const Cv = () => {
  return (
    <MarginDiv>
      <div>This is the CV component</div>
    </MarginDiv>
  );
};

export default Cv;
