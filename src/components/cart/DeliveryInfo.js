import React from 'react';
import PropTypes from 'prop-types';
import { Button, Card } from 'antd';

const DeliveryInfo = ({ items }) => {
  return (
    <Card>
      <h2>Delivery Information</h2>
    </Card>
  );
};

export default DeliveryInfo;
